{
  "name": "gpt-visualization",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.4.2",
    "vue": "^3.5.25"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=22.0.0"
  }
}
